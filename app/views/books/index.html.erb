<main class="main">
  <div class="top_page">
     <% if !user_signed_in? %>
  <div class="inner">
      <h1 >Books Intro</h1>
    <div class="inner_content">
      <div class="inner_right">
        <div class="greeting"> 
          <ul class="greeting">
            <li class="greeting">
          <%= "ここはあなたの読んだ本を紹介できるサイトです。" %>
            </li>
            <li class="greeting">
          <%= "面白かった本、読んだ本を登録できます。"%>
            </li>
            <li class="greeting">
          <%= "紹介された本を読んで感想を投稿する事ができます。"%>
            </li>
          </ul>
        </div>
      </div>
      <div class="inner_left">
          <div class="login_button">
        <%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
        </div>
        <div class="login_button">
        <%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
        </div>
      </div>
      <% else %>
      <div class="inner_page">
        <div class="user_greeting">
          <%= "こんにちは、" %>
          <%= link_to current_user.nickname, "#", class: "user_link" %> 
          <%= "さん" %>
        </div>
        <div class="book_button">
          <%= link_to '紹介する', new_book_path, class: "book_introduction" %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
  </div>


    <%# 投稿機能実装後、部分テンプレートで投稿一覧を表示する %>
    <div class="under_page">
    <div class="introduction">
      <P>紹介された図書</P>
    </div>
      <div class="books-lists">
         <% if @books.present? %>
          <% @books.each do |book| %>
      <li class='books-list'>
        <%= link_to books_path(book.id),class:"book-link" do %>
        <div class='book-img-content'>
        <%= image_tag book.image.variant(resize_to_fill: [200,200]), class: 'book-image' if book.image.attached? %>
        </div>
        <div class='book-info'>
          <h3 class='book-title'>
            <%= book.title %>
            </h3>
          <div class='book-publisher'>
            <span><%="出版社："%><%= book.publisher %></span>
          </div>
          <div class='book-genre'>
            <span><%="ジャンル："%> <%= book.genre.name %></span>
        </div>
        </div>
        <% end %>
      </li>
      <% end %>
      <% else %>
      <!-- 投稿がない場合の表示内容 -->
      <% end %>
      </div>
    </div>
</main>
